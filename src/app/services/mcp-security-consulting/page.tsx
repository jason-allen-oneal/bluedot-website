import { metadataForServicePage, renderServicePage } from '@/components/services/ServiceLandingPage'

export const metadata = metadataForServicePage('mcp-security-consulting')

export default function ServicePage() {
  return renderServicePage('mcp-security-consulting')
}
